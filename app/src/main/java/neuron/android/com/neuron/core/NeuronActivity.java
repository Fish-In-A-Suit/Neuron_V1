package neuron.android.com.neuron.core;

/**
 * Interface which defines one method (initializeImportantViews), which should be implemented by every activity which needs to initialize its views
 */
public interface NeuronActivity {
    public void initializeViews();
}
