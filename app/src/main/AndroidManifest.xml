<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="neuron.android.com.neuron">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_neuron_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_neuron_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.LoginActivity.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RegisterActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.LoginActivity.NoActionBar" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AfterGoogleSignUpActivity"
            android:theme="@style/AppTheme.LoginActivity.NoActionBar"/>
    </application>

</manifest>